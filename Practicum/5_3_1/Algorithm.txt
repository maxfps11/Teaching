	Функция main
		Аргументы: отсутствуют
1. Объявляем 2 переменные n и m типа int.
2. Считываем в переменные n и m при помощи функции scanf().
3. Объявляем 3 двумерных массива типа int
4. Используем вложенный цикл for в цикл for для движения по элементам массива и инициализации.
5. Завершаем программу кодом возврата функции additional().

	Функция additional
		Аргументы: 2 целочисленных значения и 3 двумерных массива типа int.
1. Используем вложенный цикл for в цикл for для движения по элементам массивов, сложения двух массивов и инициализации 3-его двумерного массива, и вывода результата сложения.
	1.1 Там же используем оператор if для проверки на вывод новой строки.
2. Возвращаем 0.